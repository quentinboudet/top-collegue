import { FiltrePseudoPipe } from './filtre-pseudo.pipe';

describe('FiltrePseudoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrePseudoPipe();
    expect(pipe).toBeTruthy();
  });
});
